# Secure Execution Gateway — Requirements (MVP)
# Python runtime: 3.12
#
# Notes:
# - Packages are intentionally unpinned for initial development as requested.
# - `python-magic` requires the system `libmagic` (install via apt/yum/pacman in Dockerfile).
# - Grouped sections help maintain clarity for runtime / observability / dev deps.

## Runtime
fastapi==0.128.0
uvicorn[standard]==0.40.0
pydantic==2.12.5
python-magic==0.4.27

## Observability / Metrics
prometheus-client==0.24.1

## HTTP client
httpx==0.28.1

## Testing / Linting
pytest==9.0.2
ruff==0.14.14
